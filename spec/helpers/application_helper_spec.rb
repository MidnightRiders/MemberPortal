require 'spec_helper'

describe ApplicationHelper do
  describe 'revs' do
    skip 'returns a Club that represents the Revs'
  end

  describe 'errors_for' do
    skip 'returns a %small.error with error messages if they exist'
    skip 'returns nil if no errors'
  end

  describe 'enqueue_javascript' do
    skip 'accepts a single javascript string'
    skip 'accepts an array of javascript strings'
  end

  describe 'render_javascript' do
    skip 'returns javascript_include_tag if scripts enqueued'
    skip 'returns nothing if no scripts enqueued'
  end
end
