%section
  %h3
    From the
    = link_to 'blog', 'http://www.midnightriders.com', target: 'RidersSite'
  - articles.each do |article|
    %article
      %h4= link_to article['title'], article['link'], target: 'RidersSite'
      %p
        = article['description'].gsub(/\[&#8230;\]\z/i,'')
        = link_to '… [read more]', article['link'], target: 'RidersSite'
