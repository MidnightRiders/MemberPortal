- content_for :title, 'Member Portal | Home'

.row
  .large-4.columns
    = render partial: 'users/info', locals: { user: current_user }
  .large-8.columns
    %h2.club.ne Revs Games
    .table
      .thead
        .tr
          .td Opponent / time
          .td.text-center
            = icon 'certificate'
            Pick ’Em
          .td.text-center
            = icon 'star'
            Games
      - if revs.last_match
        - m = revs.last_match
        - home = m.home_team == revs
        .tr{ class: ( 'highlight' if home )}
          .td.ne.border-left{ class: (home ? 'secondary-border' : 'primary-border') }
            = (home ? "vs #{m.away_team.name}" : "at #{m.home_team.name}")
            %small= m.kickoff.strftime('%_m.%-d %l:%M%P')
          .td.text-right
            = link_to(content_tag(:i,'',class: 'fa fa-list-ol fa-fw') + content_tag(:span,' MotM'), mot_m_path_for(m), class: 'button tiny secondary') if m.voteable?
      - revs.next_match(3).each do |m|
        - home = m.home_team == revs
        .tr{ class: ('highlight' if home)}
          .td.ne.border-left.small-5.columns{ class: (home ? 'secondary-border' : 'primary-border') }
            = (home ? "vs #{m.away_team.name}" : "at #{m.home_team.name}")
            %small= m.kickoff.strftime('%_m.%-d %l:%M%P')
          .td.no-pad.small-4.columns.text-right
            = render(partial: 'pick_ems/buttons', locals: { match: m })
          .td.text-right.small-3.columns
            = link_to(content_tag(:i,'',class: 'fa fa-question fa-fw') + content_tag(:span,' RevGuess'), rev_guess_path_for(m), class: 'button tiny secondary expand') if m.in_future?
    %h3 Other Games
    .table
      .thead
        .tr
          .td.text-center
            = icon 'certificate'
            Pick ’Em
          .td Match / time
      - @matches.each do |m|
        .tr
          .td.small-6.columns.text-right
            = render(partial: 'pick_ems/buttons', locals: { match: m })
          .td.small-6.columns.secondary-border.border-left{class: m.home_team.abbrv.downcase}
            %p
              = link_to m.kickoff.strftime('%_m.%-d %l:%M%P'), m
              %small
                at
                = m.location
